# FinSoft - Financial Management System

Современная система управления финансами, складами и производством на Vue 3 + TypeScript.

## 🚀 Технологии

- **Frontend Framework:** Vue 3 с Composition API
- **TypeScript:** Строгая типизация
- **State Management:** Pinia
- **Routing:** Vue Router с lazy loading
- **Styling:** Tailwind CSS 4
- **UI Components:** Headless UI + Heroicons
- **HTTP Client:** Axios
- **Validation:** Zod
- **Build Tool:** Vite
- **Export:** XLSX для экспорта в Excel

## 📦 Установка

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev

# Сборка для production
npm run build

# Предпросмотр production сборки
npm run preview
```

## ⚙️ Конфигурация

Создайте файл `.env` в корне проекта:

```env
VITE_API_BASE_URL=http://localhost:8000/api
VITE_APP_NAME=FinSoft
VITE_APP_ENV=development
```

## 🏗️ Структура проекта

```
finsoft_front/
├── src/
│   ├── assets/          # Статические ресурсы
│   ├── components/      # Переиспользуемые компоненты
│   │   ├── DataTable.vue
│   │   ├── LoadingSpinner.vue
│   │   ├── SkeletonTable.vue
│   │   ├── Toast.vue
│   │   └── модальные окна...
│   ├── composables/     # Переиспользуемая логика
│   │   ├── useNotification.ts
│   │   ├── useDebounce.ts
│   │   └── useExport.ts
│   ├── router/          # Конфигурация маршрутов
│   ├── services/        # API сервисы
│   │   ├── api.ts
│   │   ├── debts.service.ts
│   │   ├── warehouse.service.ts
│   │   ├── expenses.service.ts
│   │   └── workshops.service.ts
│   ├── stores/          # Pinia stores
│   │   ├── auth.ts
│   │   ├── debts.ts
│   │   ├── warehouse.ts
│   │   ├── expenses.ts
│   │   ├── workshops.ts
│   │   └── ui.ts
│   ├── types/           # TypeScript типы
│   │   ├── models.ts
│   │   ├── api.ts
│   │   └── enums.ts
│   ├── utils/           # Утилиты
│   │   ├── dateFormat.ts
│   │   ├── validation.ts
│   │   └── sanitize.ts
│   └── views/           # Страницы приложения
├── .env                 # Переменные окружения
├── .env.example         # Пример конфигурации
└── vite.config.ts       # Конфигурация Vite
```

## 🔐 Аутентификация

### Тестовые учетные данные:

**Администратор:**
- Email: `admin`
- Пароль: `admin`

**Менеджер:**
- Email: `manager`
- Пароль: `manager`

> ⚠️ **Важно:** В production версии используйте интеграцию с Laravel backend API для реальной аутентификации.

## 📡 API Интеграция

Проект настроен для работы с Laravel backend API. Все запросы автоматически включают:

- **Authorization header** с JWT токеном
- **CSRF protection**
- **Автоматический refresh токенов**
- **Обработка ошибок**

### Пример использования API:

```typescript
import { useDebtsStore } from '@/stores/debts';

const debtsStore = useDebtsStore();

// Загрузка данных
await debtsStore.fetchDebts({ page: 1, perPage: 10 });

// Создание записи
await debtsStore.createDebt({
  client: 'ООО "Компания"',
  product: 'Товар',
  totalAmount: 10000,
  currency: 'сом'
});
```

## 🎨 Основные функции

### 1. Управление долгами
- Отслеживание задолженностей клиентов
- Частичные оплаты
- История платежей
- Фильтрация и поиск

### 2. Управление складами
- Склад цеха (капсулы/стаканы)
- Склад завода (сырье)
- Учет остатков
- Контроль низких запасов

### 3. Финансы
- Доходы и расходы
- Расходы Варзоб
- Отчеты по периодам
- Экспорт в Excel

### 4. Производство
- Цех капсул
- Цех стаканов
- Учет производства
- Статистика

### 5. Китайские грузы
- Отслеживание заказов
- Статусы доставки
- Учет веса и стоимости

## 🔧 Разработка

### Добавление нового модуля:

1. **Создайте TypeScript типы** в `src/types/models.ts`
2. **Создайте API сервис** в `src/services/`
3. **Создайте Pinia store** в `src/stores/`
4. **Создайте Vue компонент** в `src/views/`
5. **Добавьте маршрут** в `src/router/index.ts`

### Валидация форм:

```typescript
import { validate, debtSchema } from '@/utils/validation';

const result = validate(debtSchema, formData);
if (result.success) {
  // Данные валидны
  await createDebt(result.data);
} else {
  // Показать ошибки
  console.error(result.errors);
}
```

### Уведомления:

```typescript
import { useNotification } from '@/composables';

const { success, error } = useNotification();

success('Запись успешно создана!');
error('Произошла ошибка при сохранении');
```

## 📊 Экспорт данных

Используйте composable `useExport` для экспорта в Excel:

```typescript
import { useExport } from '@/composables';

const { exportToExcel } = useExport();

exportToExcel(data, 'debts', 'Долги');
```

## 🔒 Безопасность

- ✅ XSS защита через санитизацию ввода
- ✅ CSRF токены
- ✅ JWT аутентификация
- ✅ Валидация на клиенте (Zod)
- ✅ Безопасные HTTP запросы

## 🚀 Production

```bash
# Сборка
npm run build

# Файлы будут в папке dist/
```

### Оптимизации:
- Code splitting по модулям
- Lazy loading роутов
- Gzip compression
- Tree shaking
- Минификация

## 📝 Лицензия

Proprietary - Все права защищены

## 👥 Команда

Разработано для FinSoft

---

**Версия:** 0.0.0  
**Последнее обновление:** Декабрь 2025
